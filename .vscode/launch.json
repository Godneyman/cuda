// .vscode/launch.json
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run CUDA",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/main.exe",
            "cwd": "${workspaceFolder}",
            "console": "externalTerminal"
        }
    ]
}